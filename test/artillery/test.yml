config:
  target: "http://localhost:3000" # Replace with your API endpoint
  phases:
    - duration: 5 # Duration of the test in seconds
      arrivalRate: 1 # Number of arrivals per second

scenarios:
  - flow:
      - get:
          url: "/api/products/5" # Replace the range with your actual product IDs
